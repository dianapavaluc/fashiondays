<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/swipeLayout"
    tools:context=".products.ProductsActivity">

    <include
        android:id="@+id/layoutList" layout="@layout/layout_list" />

    <include
        android:id="@+id/layoutLoading" layout="@layout/layout_loading" />

    <include
        android:id="@+id/layoutEmpty" layout="@layout/layout_empty" />

    <include
        android:id="@+id/layoutError" layout="@layout/layout_error" />

</androidx.constraintlayout.widget.ConstraintLayout>